json.extract! profile, :id, :user_id, :first_name, :last_name, :address, :suburb, :medicare_no, :concession_no, :date_of_birth, :sex, :created_at, :updated_at
json.url profile_url(profile, format: :json)